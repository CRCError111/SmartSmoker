-- =====================================================
-- Исправление имени файла прошивки v2.1.0
-- =====================================================
-- Проблема: В БД указано SmartSmoker_v2.1.0.bin
--           На сервере файл называется firmware_2.1.0.bin
-- =====================================================

-- Вариант 1: Обновить БД под существующее имя файла
UPDATE firmware_updates 
SET filename = 'firmware_2.1.0.bin'
WHERE version = '2.1.0';

-- Проверка
SELECT id, version, filename, file_size, is_active 
FROM firmware_updates 
WHERE version = '2.1.0';

-- =====================================================
-- Альтернатива: Переименовать файл на сервере
-- =====================================================
-- В файловом менеджере переименуйте:
-- firmware_2.1.0.bin → SmartSmoker_v2.1.0.bin
-- 
-- Тогда этот SQL не нужен!
-- =====================================================
